"""Python tooling for the DSP workspace.

The repository historically ran the :mod:`fbmx` package from inside this
directory.  This marker also makes ``python -m neural.datasets.vsco`` work
from the workspace root without changing the existing FBMX package layout.
"""

